<h1>All Movies</h1>
<ul>
  <% @movies.each do |movie| %>
    <li>
      <h2><%= movie.title %></h2>
      <p><%= movie.overview %></p>
      <img src="<%= movie.poster_url %>" alt="<%= movie.title %>" style="width: 200px; height: auto;">
    </li>
  <% end %>
</ul>
